{
  "name": "socketio-example",
  "description": "chat implementation socket.io",
  "private": true,
   "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.10.2",
    "socket.io": "^1.2.1"
  }
}
